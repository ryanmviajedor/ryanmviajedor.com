/**
 * Twitter/X uses the same card art as Open Graph. Re-exporting keeps one
 * source of truth rather than a second near-identical generator.
 */
export { alt, size, contentType, default } from "./opengraph-image";
